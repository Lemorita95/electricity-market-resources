# ELECTRICITY-DB

## content
Data acquisition clients and endpoints for ENTSO-E, EUMETSAT and COPERNICUS. Build a PostgreSQL database in a local DOCKER container. Uses FastAPI for web interface.

## reproducibility
run 
> docker-compose -f [docker-compose.yml](docker-compose.yml) up

run
> python [main.py](main.py)